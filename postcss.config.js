// POSTCSS CONFIGURATION
// This configuration defines how PostCSS processes CSS files in the project
export default {
  // PLUGINS CONFIGURATION
  // Define PostCSS plugins that transform CSS during the build process
  plugins: {
    // TAILWIND CSS PLUGIN
    // Enables Tailwind CSS functionality to generate utility classes
    tailwindcss: {},
    
    // AUTOPREFIXER PLUGIN
    // Automatically adds vendor prefixes to CSS properties for cross-browser compatibility
    autoprefixer: {},
  },
};
