// Import Radix UI's aspect ratio primitive component for maintaining consistent aspect ratios
import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio";

// Aspect Ratio component - maintains a specific width-to-height ratio for content
// This is commonly used for responsive media like images, videos, or containers
const AspectRatio = AspectRatioPrimitive.Root;

// Export the aspect ratio component for use in other parts of the application
export { AspectRatio };
